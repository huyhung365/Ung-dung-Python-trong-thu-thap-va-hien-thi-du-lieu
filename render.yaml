services:
  - type: web
    name: weather-lab-python
    runtime: python
    plan: free
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python app.py --lan --port $PORT
    healthCheckPath: /
